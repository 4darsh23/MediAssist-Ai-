# PyTorch + CUDA (install separately with the correct CUDA version):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#
# Then install the rest:
#   pip install -r requirements.txt

numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
scikit-learn>=1.3.0
Pillow>=10.0.0
seaborn>=0.12.0
tqdm>=4.65.0
onnx>=1.14.0
